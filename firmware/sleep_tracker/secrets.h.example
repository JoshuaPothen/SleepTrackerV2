// Copy this file to secrets.h and fill in your values.
// secrets.h is gitignored — never commit your real credentials.

#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials
#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"

// Vercel API endpoint (no trailing slash)
#define API_HOST      "your-app.vercel.app"
#define API_PATH      "/api/ingest"

// API key — must match INGEST_API_KEY in Vercel environment variables
#define INGEST_API_KEY "your_32_char_random_secret_here"

#endif
